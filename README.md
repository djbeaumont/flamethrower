# Flamethrower 🔥

A rust library for projecting FHIR timings.

## Development

### Running the example web UI

```shell
cargo install wasm-pack
wasm-pack build --target web
python3 -m http.server &
open http://localhost:8000/
```
